name: Temporary hotfix 317 type contracts

on:
  push:
    branches:
      - fix/317-professional-reports
    paths:
      - scripts/tmp-hotfix-317-type-contracts.mjs
      - .github/workflows/tmp-hotfix-317-type-contracts.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/317-professional-reports
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply asserted type fixes
        run: node scripts/tmp-hotfix-317-type-contracts.mjs
      - name: Remove temporary tooling
        run: rm scripts/tmp-hotfix-317-type-contracts.mjs .github/workflows/tmp-hotfix-317-type-contracts.yml
      - name: Commit product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(reports): align professional report type contracts (#317)"
          git push origin HEAD:fix/317-professional-reports
