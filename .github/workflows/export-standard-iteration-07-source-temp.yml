name: Export standard iteration 07 source temp

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/export-standard-iteration-07-source-temp.yml'

permissions:
  contents: read

jobs:
  export-source:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact PR source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create source archive
        run: |
          git archive --format=zip --output=dtsc-platform-standard-iteration-07-source.zip HEAD

      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: dtsc-platform-standard-iteration-07-source
          path: dtsc-platform-standard-iteration-07-source.zip
          if-no-files-found: error
          retention-days: 1
