import fs from "node:fs"; const f="components/enterprise/enterprise-finance-workspace.tsx", c=fs.readFileSync(f,"utf8"), failures=[]; for(const m of ["Balance générale","Grand livre","Compte de résultat","Bilan","Tableau des flux de trésorerie","Coût moyen pondéré","Publier une version non modifiable"]) if(!c.includes(m)) failures.push(`${f}: ${m}`); const labels=fs.readFileSync("lib/enterprise/i18n/business-labels.ts","utf8"); for(const m of ["finance.account.asset","finance.journal.general","finance.transaction.inflow"]) if(!labels.includes(m)) failures.push(`Dictionnaire: ${m}`); if(failures.length){console.error(failures.map(x=>`❌ ${x}`).join("\n"));process.exit(1)} console.log("✅ Français commercial vérifié sur les modules traités et les anciennes surfaces Finance ciblées.");
