import fs from "node:fs"; const e=fs.readFileSync("lib/enterprise/identity-links/expiration.ts","utf8"), r=fs.readFileSync("app/api/internal/identity-links/expire/route.ts","utf8"), v=fs.readFileSync("vercel.json","utf8"), failures=[]; for(const m of ["Math.min","take: boundedBatch","updateMany","revision: candidate.revision","invitationTokenDigest: null","Promise.allSettled"]) if(!e.includes(m)) failures.push(`Expiration: ${m}`); for(const m of ["CRON_SECRET","WORKFLOW_WORKER_SECRET","expireEnterpriseIdentityInvitations"]) if(!r.includes(m)) failures.push(`Route cron: ${m}`); if(!v.includes("/api/internal/identity-links/expire")) failures.push("Cron Vercel absent"); if(failures.length){console.error(failures.map(x=>`❌ ${x}`).join("\n"));process.exit(1)} console.log("✅ Expiration périodique vérifiée : lots bornés, idempotence, concurrence, secrets et notifications.");
